<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <LinearLayout xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="40dp"
            android:background="@color/white"
            android:orientation="vertical">

            <android.support.design.widget.TabLayout
                android:id="@+id/tab_coupon"
                android:layout_width="match_parent"
                android:layout_height="40dp"
                app:tabBackground="@drawable/ripple_one_item_bg"
                app:tabIndicatorColor="@color/login_blue_pressed"
                app:tabSelectedTextColor="@color/login_blue_pressed"
                app:tabTextColor="@color/login_blue_pressed" />

        </LinearLayout>

        <android.support.v4.view.ViewPager
            android:id="@+id/vp_coupon"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:background="#fff2f4f5"/>
        <Button
            android:id="@+id/btn_create"
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:layout_gravity="bottom"
            android:background="@drawable/coupon_check_item_bg"
            android:text="创建优惠券"
            android:textColor="#fff"
            android:textSize="@dimen/sp_16" />
    </LinearLayout>
</layout>